DATABASE_URL="postgresql://user:password@localhost:5433/dbname?schema=public"
AUTH_SECRET="change-me"

# DEMO=1 marks this as a public demo instance: auto-seeds demo data on first
# boot (if empty) AND shows the demo logins on /login. Leave unset for real
# installs (empty DB -> setup wizard).
# DEMO=1
