DATABASE_URL="postgresql://user:password@localhost:5433/dbname?schema=public"
AUTH_SECRET="change-me"

# Set to 1 only for the public demo instance — shows the demo-credentials panel on /login.
# NEXT_PUBLIC_DEMO=1
