# Automatic HTTPS via Let's Encrypt. Requires DOMAIN's DNS A/AAAA record
# pointing at this server and ports 80 + 443 reachable from the internet.
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:3000
}
